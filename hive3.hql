create table emp1(empid int,empname string,salary int,dept string) row format delimited
fields terminated by '/t' stored as textfile;

--INSERT INTO table emp1
--values(509, 'pranith', 5000000,'delivery');

--select * from emp1;
